---
'starlight-links': minor
---

Adds Firebase Studio support.

This change required lowering the minimum supported VS Code version and shipping the extension in CommonJS instead of the ES Module format.
